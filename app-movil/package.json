{
  "name": "app-movil",
  "version": "1.0.0",
  "description": "Aplicación móvil para el asistente de voz",
  "scripts": {
    "init": "npm install && npx cap init \"Asistente UML\" com.sw1.asistente --web-dir ../uml-board/dist",
    "build": "cd ../uml-board && npm run build && cd ../app-movil && npx cap sync",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor-community/speech-recognition": "^6.0.0",
    "@capacitor-community/text-to-speech": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
